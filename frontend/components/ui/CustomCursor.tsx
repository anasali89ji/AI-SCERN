/**
 * CustomCursor — DISABLED per design spec Section 10.
 * Anti-pattern: custom cursors break accessibility and feel gimmicky.
 * Replaced with a no-op component to avoid import errors.
 */
export function CustomCursor() { return null }
