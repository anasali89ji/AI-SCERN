# ─── Calibration Worker: cal-ai ─────────────────────────────────────────
# HTTP-only — NO cron trigger (saves CF free plan cron slots)
# Triggered by Worker E every 6 hours via internal fetch()
name               = "detectai-cal-ai"
main               = "workers/worker-cal-ai.ts"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]
workers_dev        = true

[[d1_databases]]
binding       = "DB"
database_name = "detectai-pipeline"
database_id   = "50f5e26a-c794-4cfa-b2b7-2bbd1d7c045c"

[vars]
ROLE = "cal-ai"
