legacy-peer-deps=true
# postcss <8.5.10 (GHSA-qx2v-qp2m-jg93) is bundled inside next@15.x itself
# Cannot fix without downgrading Next.js. Only affects build-time CSS stringify,
# not runtime user data. Accepted risk until Next.js ships a patched release.
audit-level=high
