# infra/terraform/

Not in use. Aiscern's infra is currently managed through the Vercel,
Cloudflare, and Supabase dashboards plus GitHub Actions — not Terraform.

`modules/` and `environments/` are placeholders in case that changes later;
delete this directory if Terraform is never adopted rather than letting it
sit empty indefinitely.
