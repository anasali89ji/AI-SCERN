# DISABLED — next-on-pages internally calls vercel build which requires Vercel token
# Use deploy-netlify.yml instead
name: Deploy to CF Pages (DISABLED)
on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'This workflow is disabled. Use deploy-netlify.yml'
        required: true
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled"
