# DISABLED — next-on-pages requires vercel build internally
name: CF Pages Deploy (DISABLED)
on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Disabled.'
        required: true
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled"
