# DISABLED — using Vercel native GitHub integration instead
name: Netlify Deploy (DISABLED)
on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Disabled. Use Vercel.'
        required: true
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled"
