# DISABLED — Vercel project is paused + VERCEL_TOKEN is vcp_ read-only
# Use deploy-netlify.yml instead
name: Deploy to Vercel (DISABLED)
on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'This workflow is disabled. Use deploy-netlify.yml'
        required: true
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Vercel project is paused."
