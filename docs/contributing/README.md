# docs/contributing/

Target layout, split out of the root [`CONTRIBUTING.md`](../../CONTRIBUTING.md):

- `setup.md` — local dev setup per app
- `conventions.md` — naming, commit style, branch naming (`feat/`, `fix/`, `preview/`)
- `testing.md` — how to run/verify each app (`tsc --noEmit --skipLibCheck` where Prisma/native deps block local installs, etc.)

Not written yet — placeholder only.
