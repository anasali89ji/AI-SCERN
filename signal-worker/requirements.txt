# AI-SCERN Detection Worker v4.0.0
# Install: pip install -r requirements.txt
# CPU-only PyTorch (saves ~1.5GB vs CUDA build):
#   pip install torch==2.3.0 --index-url https://download.pytorch.org/whl/cpu
# Then: pip install -r requirements.txt

# ── Core web framework ────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.17
pydantic==2.9.0
python-dotenv==1.0.1
httpx==0.27.2

# ── Image processing ──────────────────────────────────────────────────────────
numpy==1.26.4
pillow==10.4.0
scipy==1.14.1
PyWavelets==1.7.0
opencv-python==4.10.0.84
scikit-image==0.24.0
imagehash==4.3.1

# ── OCR ───────────────────────────────────────────────────────────────────────
pytesseract==0.3.13

# ── Face recognition ─────────────────────────────────────────────────────────
face-recognition==1.3.0

# ── Text engine (CPU-only PyTorch + NLP libs) ─────────────────────────────────
# NOTE: Install torch separately first to get the CPU-only build:
#   pip install torch==2.3.0 --index-url https://download.pytorch.org/whl/cpu
torch==2.3.0
transformers==4.41.0
nltk==3.8.1
textstat==0.7.3
langdetect==1.0.9

# ── GPU layers (optional — uncomment only when GPU_ENABLED=true) ──────────────
# diffusers==0.29.0
# accelerate==0.31.0
# lpips==0.1.4

# ── Utilities ─────────────────────────────────────────────────────────────────
psutil==5.9.8
