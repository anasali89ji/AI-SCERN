diff a/signal-worker/analyzers/pixel_integrity.py b/signal-worker/analyzers/pixel_integrity.py	(rejected hunks)
@@ -240,6 +240,15 @@ def clone_detection_suspicion(img_array: np.ndarray) -> tuple[float, str, float]
     gray = img_array.mean(axis=2).astype(np.float32) if img_array.ndim == 3 else img_array.astype(np.float32)
     h, w = gray.shape
 
+    # ── Quick reject (Fix #8, v4.5.0): high global texture diversity means a
+    # clone/copy-move forgery is extremely unlikely — cloned regions almost
+    # always appear against comparatively uniform backgrounds (skies, walls,
+    # skin), and complex/busy real scenes rarely need cloning to fool the eye.
+    # Skips the O(n log n) patch sort+compare entirely for the common case.
+    gray_std = float(gray.std())
+    if gray_std > 50:
+        return 0.12, f"High texture diversity (std={gray_std:.1f}) — clone unlikely", 0.0
+
     # ── Downsample to max 320px (clone detection doesn't need full res) ──────
     max_side = 320
     if max(h, w) > max_side:
